const User=require('../models/User');
const Course=require('../models/Course');
const {instance}=require('../config/razorpay');

/* exports.createPayment=await(req, res) => {
    try{
        
    }
} */